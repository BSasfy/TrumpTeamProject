public class Player {
		protected ArrayList<Card> playerDeck = new ArrayList<List<Double>>();
	protected ArrayList<Attributes> attribute = new ArrayList<List<Integer>>();
	private String name;
	
	// Player object 
	public Player (String name, ArrayList<Card> playerDeck) {
		this.name = name;
		this.playerDeck = playerDeck;
	}

	public String getName() {
		return name;
		
	}
	public ArrayList<Card> getCards(){
		return playerDeck;
	}
	
	public Object getCard() {
		Object b = playerDeck.get(0); // as always top card
		return b;
	}
	public ArrayList<List<Integer>> getAttributeList() {
		return attribute;
	}
	// make turn ?
	public void takeTurn() {
		
	}
	//finish turn?
	public void finishTurn(){
		
	}
	
	// get category
	public double getAttribute(int i) {
		double b = attribute.get(i).get(i);
			}
		return b;
	}
	
	// get player status
	public Status getActive() {
		getActive();
		return Status;
	}
	public Status notActive() {
		return Status;
	}
	// some code for database?
	public int isWinner() {
		int count= 0 ;
		int a = 0;
		count = a +1;
		count++;
		return count;
	}
	

}
	
